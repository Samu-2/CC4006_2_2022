#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

vector<ll> s;
vector<ll> v;

int main() {
    int n; cin >> n;
    s.resize(n); for(auto &x: s) cin >> x;
    v.resize(n); for(auto &x: s) cin >> x;

    

    return 0;
}